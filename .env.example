# Placeholders only. Never commit real credentials.
FACTORY_ENV=local
# Optional future gateway access (not loaded automatically; set environment variables
# or use the Git-ignored .ai/automation/credentials.json described in BOOTSTRAP-AUTOMATION.md).
FACTORY_OMNIROUTE_API_KEY=
# Only for an authenticated NVIDIA free developer/trial account, never a paid endpoint.
FACTORY_NIM_FREE_TIER=
